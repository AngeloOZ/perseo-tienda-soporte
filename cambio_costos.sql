UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (1);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);

    UPDATE productos_homologados_distribuidor2 
SET
    precioiva = preciobase * 1.21, 
    precioivabase = preciobase * 1.21,
    precio = preciobase,
    preciobase = preciobase
WHERE
    id IN (123);
